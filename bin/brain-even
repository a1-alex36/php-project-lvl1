#!/usr/bin/env php
<?php
// import либы эвент
// автозагрузка функций.

use function Brain\Even\Games\Even\Run;

$autoloadPath1 = __DIR__ . '/../../../autoload.php';
$autoloadPath2 = __DIR__ . '/../vendor/autoload.php';
if (file_exists($autoloadPath1)) {
    require_once $autoloadPath1;
} else {
    require_once $autoloadPath2;
}

//вызов метода запуск игры. Или задать вопрос
Run();

// это консольный скрипт. Только вызов модуля
//
//-------что не делать тут

//-------что  делать тут
//надо ли делатьт парсинг аргументов командной строки ?
//это Можно тут. или отдельный модуль (н связанные с логикой доменной)

//* файл модуля
// без вызова своих процессов/методов
// без побочных эффектов - типа вывода на экран
// только интерфейс
// итого больше становится похоже на набор методов


//* файл логики
// вызов методов модуля
// принять ввод
// отдать вывод
// можно ифы, или стратегию, или полиморфный замут


// сделать как-то.
//в след шаге выделить общую логику. обдумать заново, переделать